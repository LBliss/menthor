import menthor.Vertex

package object algorithm
{
  type ItemID = Int // with ItemID
  type UserID = Int // with UserID
  type Grade = Int // with Grade
  type WeightedGrade = Double
  type Similarity = Double
  type User = Vertex[DataWrapper]
}